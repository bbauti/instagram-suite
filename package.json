{
  "name": "instagram-suite",
  "version": "1.0.0",
  "description": "One paste-into-console SPA with three Instagram tools (Ledger, Followers, Pending) over one shared core.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --minify --charset=utf8 --legal-comments=none --outfile=dist/instagram-suite.js --banner:js=\"/* Instagram Suite — generated from src/. Do not edit this file; edit src/ then run: npm run build */\"",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "lit-html": "^3.3.3"
  }
}
